package Neuranet.CNN;

/**
 * Enum that delineates the type of pooling
 * that a convolutional neural network uses.
 * @author Nolan Bridges
 * @version 1.0.0
 */
public enum Pooling {
    MAX,
    AVERAGE
}
